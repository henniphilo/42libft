void	*ft_memchr (const void *block, int c, size_t size)
{
    const unsigned char     *str;

    str = s;
    while (n--)
    {
        if (*str == (unsigned char)c)
            return (str);
            str++;
    }
    return (NULL);
}
